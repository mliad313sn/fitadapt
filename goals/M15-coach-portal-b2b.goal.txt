/goal Implement M15 Coach Portal (B2B) as specified in docs/specs/M15-coach-portal-b2b.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) apps/coach-web supports sign-in, client roster, assigning a program, editing a session, reviewing logs and pain flags, and messaging; Playwright E2E tests cover each flow. (2) RBAC tests: owner/coach/assistant permissions; a coach sees only data within the client's SharedScope; revocation takes effect immediately. (3) A coach edit that would exceed a client's SafetyProfile caps is rejected by the engine with an explanation (test). (4) Every coach action writes an audit entry (test). (5) The portal meets WCAG 2.2 AA on core pages (axe checks in Playwright). (6) L10 tests: a coach cannot access any client before accepting the Coach Agreement and passing credential verification; clients see the verification status. (7) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (8) docs/status/M15.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; No coach may lift safety caps. If blocked, or after 70 turns, stop and update docs/status/M15.md with the remaining gaps and exact next steps.
