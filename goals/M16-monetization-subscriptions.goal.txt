/goal Implement M16 Monetization & Subscriptions as specified in docs/specs/M16-monetization-subscriptions.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) apps/api has an entitlement service fed by webhook fixtures for purchase, renewal, cancellation, refund and restore; integration tests cover each. (2) Duo plan: two seats linked; removing a seat revokes entitlements (test). (3) A test enumerates safety features (screening, pain monitor, red-flag flow, data export) and proves they are available on the Free tier. (4) Tampering test: modifying local entitlement state does not unlock premium API features. (5) Paywall screens are localised FR/EN and support regional price display from store data (component tests). (6) L8 tests: cancellation takes no more steps than purchase; a trial-end reminder is scheduled; displayed prices include taxes per store data. (7) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (8) docs/status/M16.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; No real payment credentials; sandbox fixtures only. If blocked, or after 50 turns, stop and update docs/status/M16.md with the remaining gaps and exact next steps.
