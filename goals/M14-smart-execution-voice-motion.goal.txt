/goal Implement M14 Smart Execution — Voice Coach & On-Device Motion Sensing as specified in docs/specs/M14-smart-execution-voice-motion.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) packages/motion implements a rep-counting state machine per movement, tested on recorded pose-keypoint fixtures (≥ 50 sets per movement from consenting testers or synthetic sequences) with ≥ 95% count accuracy. (2) A benchmark note in docs/status/M14.md reports ≥ 20 fps on the reference mid-range device (manual measurement recorded with device name). (3) A network-spy test proves zero network calls while camera mode is active; no frames are written to storage. (4) Voice cues play in FR and EN for every cue key; voice commands 'done N reps', 'rest', 'next' are parsed by unit-tested intent rules. (5) Camera mode requires an explicit per-session start and shows a persistent indicator (component test). (6) The first camera session shows the L3 camera notice; a test proves no pose or image data is persisted. (7) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (8) docs/status/M14.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Label form feedback as beta; never claim injury prevention. If blocked, or after 70 turns, stop and update docs/status/M14.md with the remaining gaps and exact next steps.
