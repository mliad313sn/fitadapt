/goal Implement M07 Assessment & Benchmark Testing as specified in docs/specs/M07-assessment-benchmarks.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) packages/engine/assessment defines home, gym and 55+ protocols; a guided assessment screen runs them offline. (2) Mapping tests: each result maps to the correct ladder rung and starting load; zero results map to the lowest rung; e1RM from RIR-adjusted Epley is correct for 1–12 reps. (3) Gating test: allowMaxTests=false → every test instruction stops at RIR 2. (4) Integration test: completing an assessment for persona P1 and P5 produces a CapacityModel that generateSession() uses for the first session. (5) A re-assessment prompt appears at the end of a mesocycle (fake clock). (6) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (7) docs/status/M07.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Never instruct a maximal or to-failure test to a gated user. If blocked, or after 40 turns, stop and update docs/status/M07.md with the remaining gaps and exact next steps.
