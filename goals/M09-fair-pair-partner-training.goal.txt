/goal Implement M09 Fair Pair — Partner & Group Training as specified in docs/specs/M09-fair-pair-partner-training.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) packages/engine/pair exports mergePlans(planA, planB, sharedEquipment) → SharedTimeline; tests prove same pattern per block, individual variant/load, and rest windows honoured for both. (2) Fairness tests: for P1 (120 kg) and P2 (60 kg) performing equal relative effort, Fair Challenge Scores differ by ≤ 5%; property tests across random pairs confirm the same. (3) Safety tests: each participant's SafetyProfile and red joints apply independently in the shared timeline. (4) A Maestro E2E test runs a single-device pair session offline with alternating turns and per-person logging. (5) Integration test: two clients join a multi-device session over WebSocket, stay in sync through a disconnect/reconnect, and end with correct per-user logs. (6) Bodyweight is hidden from the partner view unless the owner opts in (test). (7) Consent tests: each participant must have accepted their own L2 documents; no data is shared with a partner without that user's sharing consent; challenge scoring is off by default. (8) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (9) docs/status/M09.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Single-device mode must require no network. If blocked, or after 70 turns, stop and update docs/status/M09.md with the remaining gaps and exact next steps.
