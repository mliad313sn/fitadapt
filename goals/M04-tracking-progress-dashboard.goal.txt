/goal Implement M04 Tracking, Analytics & Progress Dashboard as specified in docs/specs/M04-tracking-progress-dashboard.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) packages/engine/analytics exposes pure functions for e1RM history, volume load, weekly hard sets per muscle, EWMA bodyweight trend, %BW/week rate, adherence and milestone forecasts (returning a date range and confidence); unit tests cover each. (2) A test proves sustained loss > 1% BW/week for 3 weeks emits the guardrail event consumed by M10 (or a stub if M10 is not built). (3) The dashboard renders a seeded two-year dataset in < 1 s in a performance test on the reference profile. (4) Progress photos are encrypted at rest; a test with a network spy proves zero uploads while backup is off; enabling backup uploads only ciphertext. (5) CSV and JSON export includes all training and body data and round-trips through the importer in tests. (6) A copy test fails on a denylist of judgemental body terms in FR and EN catalogues. (7) Forecast components always render an 'estimate, not a guarantee' label in FR and EN (component test). (8) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (9) docs/status/M04.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Do not implement progression logic here; read it from packages/engine. If blocked, or after 50 turns, stop and update docs/status/M04.md with the remaining gaps and exact next steps.
