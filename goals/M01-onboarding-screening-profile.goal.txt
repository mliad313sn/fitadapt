/goal Implement M01 Onboarding, Health Screening & Dynamic Profile as specified in docs/specs/M01-onboarding-screening-profile.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) The onboarding flow follows the spec's screen order; a Maestro (or Detox) E2E test completes onboarding and lands on the first-workout screen in ≤ 12 screens, once in FR and once in EN. (2) packages/safety exports evaluateScreening(responses) returning a SafetyProfile; table-driven tests cover all-no, every single yes, pregnancy, age < 16 blocked, age 16–17 deficit disabled, and 'advised against calorie restriction'. (3) The SafetyProfile is persisted, synced and exposed through a typed selector; a fake-clock test proves a re-screen prompt appears after 12 months. (4) A user can create at least 3 equipment profiles; an integration test proves switching profile changes the queried exercise pool (use the M06 seed if present, otherwise a fixture). (5) An E2E path skips weight and body fat and still reaches the first workout. (6) Screening wording lives in content files marked 'licence check pending'; every input has an accessibility label. (7) L2 test: the first workout is unreachable until current Terms, Privacy Policy, health-data consent and exercise-risk acknowledgment are accepted; the API stores version, locale, jurisdiction and timestamp; publishing a new material version forces re-acceptance (E2E + API tests). (8) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (9) docs/status/M01.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; No diagnostic wording anywhere. Do not weaken any SafetyProfile field to make a test pass. If blocked, or after 50 turns, stop and update docs/status/M01.md with the remaining gaps and exact next steps.
