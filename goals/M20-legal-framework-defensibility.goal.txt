/goal Implement M20 Legal Framework, Claims Control & Defensibility as specified in docs/specs/M20-legal-framework-defensibility.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) packages/legal provides versioned documents per locale/jurisdiction, an acceptance service and a notice registry; tests cover acceptance, re-acceptance on material change and jurisdiction-specific age thresholds. (2) A production build config test fails if any enabled legal text lacks counsel approval. (3) 'pnpm legal:claims' lints i18n catalogues, store/ metadata and AI-coach prompts against FR/EN denylists (diagnose, treat, cure, guaranteed results, burn fat fast and French equivalents); a fixture with a forbidden claim fails and an entry in the substantiation file passes. (4) 'pnpm legal:licences' generates an SBOM, fails on licences outside the allowlist, and validates the asset/dataset licence register; both run in CI. (5) The defensibility log is append-only and hash-chained; a test proves tampering is detected, and 'pnpm legal:export --user <id>' produces a legal-hold export of acceptances, notices, safety events and engine versions. (6) docs/legal contains the founder checklist, jurisdiction matrix, risk register, document list with drafts, trademark register, incident procedure and counsel sign-off tracker, each marked 'requires counsel review'. (7) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (8) docs/status/M20.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Write legal texts only as clearly marked drafts; never mark anything counsel-approved. If blocked, or after 55 turns, stop and update docs/status/M20.md with the remaining gaps and exact next steps.
