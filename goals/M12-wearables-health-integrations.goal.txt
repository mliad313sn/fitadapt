/goal Implement M12 Wearables & Health Integrations as specified in docs/specs/M12-wearables-health-integrations.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) packages/integrations defines a HealthSource interface with HealthKit and Health Connect adapters; mocked tests cover reading each data type and writing a workout. (2) The BLE heart-rate parser handles 8-bit and 16-bit HR formats and RR intervals from recorded packet fixtures. (3) Consent tests: reads happen only for consented types; revocation stops reads and deletes cached samples of that type. (4) Readiness integration test: HRV/RHR inputs change the M05 readiness score; removing them falls back to self-report without errors. (5) A settings screen lists connections and consents in FR/EN. (6) L9 test: a network/analytics spy proves health-platform data never reaches analytics or third-party endpoints. (7) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (8) docs/status/M12.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; No background location or continuous tracking. If blocked, or after 50 turns, stop and update docs/status/M12.md with the remaining gaps and exact next steps.
