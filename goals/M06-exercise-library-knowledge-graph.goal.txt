/goal Implement M06 Exercise Library & Knowledge Graph as specified in docs/specs/M06-exercise-library-knowledge-graph.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) packages/shared defines zod schemas for Exercise, ExerciseEdge, Equipment and Muscle exactly as in the spec. (2) A seed of ≥ 120 exercises with FR and EN names, cues and common mistakes validates against the schema; every seed entry has validated:false and reviewStatus:'pending'. (3) Graph tests: progression chains acyclic; every PROGRESSES_TO has a matching REGRESSES_TO; ≥ 90% of exercises have a substitute available with bodyweight-only, home-basic and full-gym equipment sets; ladders for pull, push and squat from M02's spec are complete. (4) substitute(exercise, equipment, jointFlags, safetyProfile) returns the highest-similarity valid option; tests prove it never returns an exercise loading a red joint at medium/high or needing missing equipment; p95 query time < 20 ms on the seed. (5) The library is available offline in SQLite on device; search and filter by pattern, muscle and equipment work in a component test. (6) L6 test: publishing a MediaAsset without licence, source and rightsHolder fails. (7) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (8) docs/status/M06.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Do not present seed content as expert-validated. Use placeholder media only; no copyrighted videos or images. If blocked, or after 60 turns, stop and update docs/status/M06.md with the remaining gaps and exact next steps.
