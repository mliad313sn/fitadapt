/goal Implement M05 Recovery, Mobility & Pain-Monitored Safety as specified in docs/specs/M05-recovery-mobility-pain-safety.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) Warm-up generator tests: duration 5–8 minutes, mobility matches the day's movement patterns, ramp-up sets precede the first heavy lift. (2) Pain model tests: 0–3 green, 4–5 amber, ≥ 6 or unsettled next morning red; a red joint makes the next generateSession() substitute every medium/high-load exercise on that joint (S2). (3) Deload trigger tests for each trigger in the spec; volume reduced 40–50%. (4) A Maestro E2E test reports chest pain mid-session: the session ends, seek-care guidance appears, and an intensity lock persists until attestation (S3). (5) Readiness tests: low score trims volume by one set per exercise; missing wearable data never blocks. (6) A persistence test recommends a physiotherapist after > 2 weeks of amber/red on the same joint. (7) Red-flag events are written to the defensibility log; emergency guidance is read from per-jurisdiction config (test with two jurisdictions). (8) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (9) docs/status/M05.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; No diagnostic wording; all medical-style copy goes in content files flagged for physician review. If blocked, or after 50 turns, stop and update docs/status/M05.md with the remaining gaps and exact next steps.
