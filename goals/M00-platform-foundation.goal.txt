/goal Implement M00 Platform Foundation & Design System as specified in docs/specs/M00-platform-foundation.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) The monorepo layout in the spec exists; 'pnpm install' then 'pnpm -w build' succeed. (2) 'docker compose up -d' starts PostgreSQL and Redis; 'pnpm --filter api test:integration' passes tests for one-time-code sign-up, sign-in, refresh-token rotation (reuse of an old refresh token is rejected) and logout. (3) packages/sync tests prove: a record created offline is written to the outbox, pushed on reconnect, pulled on a second simulated device; a duplicate push is idempotent; concurrent append-only set logs from two devices both survive. (4) The Expo app boots; a test renders the home screen in FR and EN; an ESLint rule fails on a fixture containing a hard-coded JSX string. (5) packages/ui ships tokens (light/dark) and Button, Card, NumberDisplay, Timer, Stepper, Input, Sheet, Toast with tests asserting accessibility labels and minimum touch targets (48 dp standard, 56 dp gym mode). (6) A GitHub Actions workflow runs typecheck, lint and test on pull requests; eas.json defines dev, preview and production profiles. (7) docs/adr contains ADR-001 stack, ADR-002 sync, ADR-003 auth. (8) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (9) docs/status/M00.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Build no fitness features. Commit only .env.example, never real secrets. If blocked, or after 60 turns, stop and update docs/status/M00.md with the remaining gaps and exact next steps.
