/goal Implement M18 Analytics, Experimentation & Admin CMS as specified in docs/specs/M18-analytics-experimentation-admin.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) A typed event taxonomy exists; a test rejects events containing health values or free text. (2) Feature flags and A/B assignment are deterministic per user and tested. (3) Remote-config tests: a coefficient change requires two approvals, rolls out by percentage, and rolls back to the previous version; S1–S7 thresholds are rejected as non-configurable. (4) The admin CMS (inside apps/coach-web or apps/admin) supports create → review → publish for exercises and translations with two-person approval (Playwright E2E); every admin action is audited. (5) A retention and funnel dashboard definition is committed (queries or config) for activation, adherence and W4 retention. (6) Config test: experiments or flags targeting acceptance, notices, AI disclosure, cancellation or safety flows are rejected. (7) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (8) docs/status/M18.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Safety thresholds are code, not config. If blocked, or after 55 turns, stop and update docs/status/M18.md with the remaining gaps and exact next steps.
