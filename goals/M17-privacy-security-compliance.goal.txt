/goal Implement M17 Privacy, Security & Compliance as specified in docs/specs/M17-privacy-security-compliance.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) Consent records are versioned per data type; tests prove features for a data type stay off without consent and switch off on withdrawal. (2) In-app export returns all user data (JSON) and deletion removes it from primary storage immediately with a scheduled backup purge; integration tests cover both. (3) A log/analytics scrubber test fails if emails, names, weights, pain reports or free text reach logs or analytics events. (4) CI runs dependency audit, secret scanning and static analysis; the pipeline fails on high/critical findings. (5) docs/compliance contains a DPIA draft, record of processing, retention schedule, breach runbook and a MASVS/ASVS checklist with status per item. (6) The age gate blocks users under 16 (E2E). (7) docs/compliance/records-of-processing includes lawful basis, transfer mechanism and authority-filing status per jurisdiction in the matrix. (8) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (9) docs/status/M17.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Legal documents are drafts marked 'requires counsel review'. If blocked, or after 60 turns, stop and update docs/status/M17.md with the remaining gaps and exact next steps.
