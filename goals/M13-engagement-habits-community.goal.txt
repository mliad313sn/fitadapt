/goal Implement M13 Engagement, Habits & Community as specified in docs/specs/M13-engagement-habits-community.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) Streak tests: planned rest days keep streaks; freezes work; missing an unplanned day breaks the streak without guilt copy (copy denylist test FR/EN). (2) Notification scheduler tests: ≤ 1 per day by default, quiet hours honoured, reminders derived from the user's plan. (3) Achievements fire on PRs, variant unlocks and consistency milestones (unit tests with seeded logs). (4) Private challenge between two users works end to end (API integration test); invite and referral links attribute correctly. (5) Share-card test: rendered image metadata and content exclude bodyweight, photos and location unless opted in. (6) Tests: marketing notifications need a separate opt-in; report and block work on challenge content and create a moderation-queue item. (7) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (8) docs/status/M13.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; No public leaderboards; no dark patterns (confirm-shaming, forced continuity). If blocked, or after 45 turns, stop and update docs/status/M13.md with the remaining gaps and exact next steps.
