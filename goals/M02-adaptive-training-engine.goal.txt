/goal Implement M02 Adaptive Training Engine & Session Execution as specified in docs/specs/M02-adaptive-training-engine.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) packages/engine exports generateSession() and evaluateProgression() with zod-typed input/output; a test proves the package has no I/O imports and uses only the injected clock and seed. (2) Golden-file tests for the six personas in docs/specs/00-product-vision.md produce stable plans committed as snapshots. (3) fast-check property tests over ≥ 10,000 random valid inputs prove: SafetyProfile caps never exceeded; weekly e1RM-based load increase ≤ 10% per exercise; planned duration ≤ minutes available; no exercise needs equipment missing from the active profile; no medium/high load on a red-flagged joint. (4) Unit tests cover every rule in the spec's Rules section, including bodyweight ladder promotion/regression and increment rounding for plates, dumbbell pairs and machine stacks. (5) Every PlannedSet has ≥ 1 reason code; a test renders all reason codes in FR and EN with no missing key. (6) A Maestro E2E test executes a full session in offline mode: logging reps/load/RIR in ≤ 2 taps per set, swap, skip and pain flag; a fake-timer test proves the rest timer stays correct after backgrounding. (7) Coefficients live in packages/engine/config with 'source' and 'validated' fields; line coverage of packages/engine ≥ 95%. (8) L4/L11 tests: stop and skip exist in every execution-screen state; the first session shows the L3 notice; each executed prescription writes engineVersion and reason codes to the defensibility log. (9) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (10) docs/status/M02.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; No machine learning and no network access inside packages/engine. Unvalidated coefficients must stay marked validated:false and be listed in the status file. If blocked, or after 90 turns, stop and update docs/status/M02.md with the remaining gaps and exact next steps.
