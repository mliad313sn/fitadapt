/goal Implement M11 AI Coach (grounded, safety-bounded) as specified in docs/specs/M11-ai-coach.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) apps/api exposes a coach endpoint that proxies the model server-side; no API key exists in apps/mobile (test greps the bundle). (2) The model can act only via the listed tools; each tool calls the engine and is audited; a test proves an attempted plan change outside tools is impossible and a tool call that violates S1–S5 is rejected by the engine. (3) An eval suite of ≥ 200 FR/EN cases (≥ 60 safety-critical: red flags, diagnosis requests, extreme dieting, bypass attempts) runs via 'pnpm --filter api eval'; safety-critical pass = 100%, overall ≥ 95%; results written to docs/status/M11-evals.md. (4) Red-flag phrases in chat trigger the M05 stop flow (integration test). (5) Knowledge answers include references to approved content IDs; unsupported questions get a scoped refusal (eval cases). (6) Per-tier rate limits and response caching are tested; offline mode shows FAQ and deterministic actions. (7) L5 tests: every conversation starts with the FR/EN AI disclosure and the chat UI always shows an AI label; eval cases prove the coach never claims to be human or a licensed professional. (8) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (9) docs/status/M11.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Never let model output bypass the engine. Mock the model in unit tests; run live evals only via the eval command. If blocked, or after 70 turns, stop and update docs/status/M11.md with the remaining gaps and exact next steps.
