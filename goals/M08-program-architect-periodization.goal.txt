/goal Implement M08 Program Architect — Periodization & Scheduling as specified in docs/specs/M08-program-architect-periodization.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) packages/engine/program generates a Program with split, mesocycles and weekly hard-set targets for each persona; golden tests committed. (2) Split-selection tests cover 2 to 6 days/week for every goal. (3) Reflow tests: missing any single session produces a valid week with ≤ 1 extra session and never two hard sessions for the same pattern on consecutive days. (4) Concurrent-training tests: when avoidable, HIIT is not scheduled the day before a heavy lower-body session. (5) A calendar screen shows the week and applies reflow offline; copy after a missed session passes a 'no guilt' denylist test in FR and EN. (6) Deload weeks are scheduled every 4–6 weeks and exposed to M05. (7) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (8) docs/status/M08.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Volume targets stay in config with source and validated:false. If blocked, or after 50 turns, stop and update docs/status/M08.md with the remaining gaps and exact next steps.
