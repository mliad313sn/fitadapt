/goal Implement M03 Cardio & Conditioning Suite as specified in docs/specs/M03-cardio-conditioning.md, following CLAUDE.md, the safety invariants S1–S7 in docs/specs/00-product-vision.md and the legal invariants L1–L12 in docs/specs/00-legal-framework.md (read all four first). The goal is met only when every condition below is true AND demonstrated in this session by pasting the output of the commands that prove it: (1) packages/engine/cardio generates HIIT, Tabata, EMOM, AMRAP and steady-state sessions; unit tests cover each protocol's timeline. (2) A fake-timer test proves interval timer drift < 100 ms over 20 minutes; the mobile timer plays FR/EN audio cues and haptics with the app backgrounded (native module or expo-av test harness plus a manual-test note in the status file). (3) Zone tests: HRmax = 208 − 0.7 × age; Karvonen zones from resting HR; RPE/talk-test fallback when no HR data. (4) Gating tests: allowHIIT=false or < 2 weeks of logs → no HIIT; knee/ankle/hip flag or BMI ≥ 35 → only low-impact exercises by default. (5) A weekly aerobic-minutes ledger counts vigorous minutes double and is shown against 150–300 min. (6) A test scans the FR and EN catalogues and fails if 'fat-burning zone', 'maximum lipolysis' or their French equivalents appear. (7) L3 test: the first HIIT session cannot start before the intensity notice is shown and confirmed. (8) 'pnpm -w typecheck', 'pnpm -w lint' and 'pnpm -w test' all exit 0 on a clean run. (9) docs/status/M03.md lists what was built, ADRs added, deviations from the spec with reasons, every item left validated:false for expert review, and open questions. Constraints: never weaken S1–S7 or L1–L12; never add medical or results claims or unlicensed assets; never bypass acceptance, notices or AI disclosure; never delete, skip or loosen existing tests to get green; no secrets in git; every user-facing string goes through i18n FR/EN; Timers run on-device only; no server-side timer state. If blocked, or after 50 turns, stop and update docs/status/M03.md with the remaining gaps and exact next steps.
